# API keys — never commit
.env

# Claude Code local settings (contains tool-call history with API keys)
.claude/

# Python
__pycache__/
*.pyc

# Cache data
data/cache/

# Saved transcripts (local only)
data/transcripts/

# Portfolio data (stored in browser localStorage per user)
data/portfolio.json

# OS files
.DS_Store
